{
  "name": "miniconf-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "test": "vitest run --exclude src/lib/backend.integration.test.ts",
    "test:integration": "vitest run src/lib/backend.integration.test.ts"
  },
  "dependencies": {
    "mqtt": "^5.15.2",
    "svelte": "^5.56.9"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.11"
  }
}
